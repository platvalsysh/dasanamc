# 통합 검토 리포트 — dasanamc (2026-05-19 스냅샷)

4개 영역 (보안 / 아키텍처 · 코드 품질 / DB · 마이그레이션 / DevOps · 관측성) 병렬 감사 결과 합성. 같은 항목이 여러 영역에서 잡힌 경우 우선순위 가산.

기준 커밋: `79e7284` (develop). 같은 항목을 두 번 이상 발견했다면 단일 행으로 통합.

---

## ⚠️ 즉시 처리할 부수 위험 (감사 부산물)

보안 감사 에이전트가 로컬 `.env` 파일을 읽어 `SUPABASE_SERVICE_ROLE_KEY` 와 DB 패스워드를 응답에 평문 인용했음. `.env` 자체는 gitignored 되어 커밋되지는 않았으나 **두 시크릿 모두 Supabase Dashboard 에서 회전(rotate) 권장**. 이번 세션 로그가 외부 저장되는 환경이면 유출로 간주.

회전 절차:
1. Supabase Dashboard → Project Settings → API → service_role 키 재발급
2. Settings → Database → 비밀번호 재설정
3. 로컬 `.env` 의 `SUPABASE_SERVICE_ROLE_KEY`, `DATABASE_URL`, `DIRECT_URL` 갱신
4. Vercel 환경변수도 함께 갱신 (배포 중이라면)

---

## 🚨 Critical — 외주 출시 전 반드시

| # | 항목 | 위치/근거 | 영향 | 노력 |
|---|---|---|---|---|
| C1 | **모든 admin POST 액션에 권한 체크 없음** — 일반 회원 로그인만 하면 사용자 비밀번호 강제 변경, 자기 자신에게 super_admin 부여, hard-delete, 게시판 영구삭제 모두 가능 | `apps/web/app/layouts/admin.tsx:15-18` 가 `isLogged` 만 체크. 각 action 첫 줄에 `auth.checkPermissions([...])` 없음 (`packages/auth/src/admin/users/page.tsx`, `roles/page.tsx`, `assign-roles/page.tsx`, `packages/admin/src/routes/menu.tsx`, 보드 `delete.tsx`, sms `page.tsx`) | 권한 escalation = 시스템 탈취 | M |
| C2 | **`AUTH_SECRET` 기본 fallback** — 휴대폰 인증 HMAC 시크릿이 미설정 시 `"default-secret-for-phone-verify"` 사용 | `packages/auth/src/.server/phone-verify.ts:14` | 누구나 verified 쿠키 위조 → 회원가입 시 휴대폰 인증 우회 | S |
| C3 | **`$queryRawUnsafe` 사용** | `packages/admin/src/routes/menu.tsx:68` | C1 결합 시 임의 SQL 실행 | S |
| C4 | **MD5 fallback 자동 업그레이드 로직** — 로그인 시 입력 password 의 MD5 가 DB 와 일치하면 통과 후 bcrypt 재해싱 | `packages/auth/src/public/login/page.tsx:34-52` | MD5 깨진 해시. chemeng 레거시 데이터 마이그용이면 dasanamc 엔 불필요 | S |
| C5 | **`handle_new_user_minimal()` 가 anon/authenticated 에 RPC 노출 + `search_path` mutable** | `packages/core/migrate/099_create_profile_trigger.sql:23-53`, Supabase advisor lint 0011 / 0028 / 0029 | SECURITY DEFINER 라 임의 user_id 로 프로필 위조 가능 | S |
| C6 | **`core.identifiers.identifier` UNIQUE 인덱스 없음** — 로그인 ID 컬럼인데 PK 가 `user_id` 뿐 | `packages/core/migrate/000_init.sql:64-68` | 동일 ID 중복 등록 → 로그인 동작 불확정 | S |
| C7 | **SMS / 휴대폰 인증 / 로그인에 rate limit 0건** | `packages/auth/src/.server/phone-verify.ts`, `packages/module-sms` | 외주 클라이언트 SMS 비용 폭격 + brute force 가능 | M |

---

## 🔴 High — 다음 1-2 스프린트

| # | 항목 | 위치/근거 | 노력 |
|---|---|---|---|
| H1 | **CI 파이프라인 0개**. Vercel 만이 게이트인데 `--prod` 라 typecheck 실행 안 됨 | `.github/` 부재. PR 마다 typecheck/test/eslint/syncpack:check 자동화 필요 | S (반나절) |
| H2 | **Pre-commit hooks 0개**. husky/lint-staged 미도입 | `.husky/` 부재. commit 시점에 lint --fix + prettier --check | S (반나절) |
| H3 | **에러 모니터링 0개**. 프로덕션 500 / SSR 실패 알 길 없음 | Sentry 또는 `@sentry/react-router` 도입 권장 | M (1일) |
| H4 | **chemeng 컨텐츠 광범위** — 외주 매번 일일이 찾아 바꿔야 함 | 홈/about/events/familysites/activities/membership + footer + hero + 멤버십 페이지의 실제 계좌/연락처/이메일. `site-menu.ts` / `admin-menu.ts` 기본값도 chemeng 또는 한 번도 만들지 않은 e-commerce 메뉴 | L |
| H5 | **`packages/auth/src/types/permissions.ts` 478줄 거대 데드코드** — chemeng 의 admins/orders/products/analytics/reports 권한 + 6개 역할 하드코딩. `generate-permissions.ts` 와 양방향 동기화 끊김 | 정리 시 `init-permissions.ts` 도 함께 손봐야 | M |
| H6 | **역방향 의존: `@repo/auth` → `@repo/module-sms`** — INDEX.md 의존 규칙 위반. ESLint 룰이 module→module 만 막아 못 잡음 | sign-up/mypage 의 휴대폰 인증 부분. `@repo/shared-sms` 추출 또는 어댑터 인터페이스로 분리 | M |
| H7 | **헬스체크 엔드포인트 없음** | `/api/health` 추가 (`prisma.$queryRaw` `SELECT 1`) | S (1시간) |
| H8 | **FK 인덱스 5개 누락** — `admin_role_permissions.permission_id`, `admin_user_roles.role_id`/`granted_by`, `comments.parent_id`, `document_ip_view_log.document_id` | Supabase performance advisor 0001 | S |
| H9 | **`(user_id, is_active)` 부분 인덱스 누락** — JWT 발급마다 호출되는 `custom_access_token_hook` 의 핵심 쿼리 | `CREATE INDEX ON core.admin_user_roles(user_id) WHERE is_active = true;` | S |
| H10 | **`turbo.json globalEnv` 불완전** — `.env.example` 의 절반 누락 → Vercel 빌드 캐시 부정확 | `VITE_*`, `SMS_*`, `ENABLED_MODULES`, Solapi 자격증명 등 모두 추가 | S |
| H11 | **`update_updated_at_column()` `search_path` mutable** | `099_create_profile_trigger.sql`. `SET search_path = ''` 추가 | S |
| H12 | **storage public buckets 의 SELECT 정책이 LIST 노출** | `public-images`, `public-videos` 정책. SELECT 정책 자체 제거 또는 prefix 제한 | S |

---

## 🟡 Medium — 다음 한 달

| # | 항목 | 위치/근거 |
|---|---|---|
| M1 | **`UserTable.tsx` 974줄 / `sign-up` 855 / `mypage` 1002 / `users/page` 937** — 한 파일에 폼/모달 5종/검증/Supabase admin 호출. PR 충돌과 인지부하 큼. 모달 단위 분리 |  |
| M2 | **admin assign-roles 의 `granted_by` 미기록** — 주석 "we should try" 만 남고 NULL 저장 | audit trail 누락 |
| M3 | **이메일 변경 시 Supabase auth.users 동기화 누락** — `prisma.users.update` 로 email 만 변경. auth 계정과 어긋남 → 로그인 불가 | `auth/admin/users/page.tsx:305-315` |
| M4 | **forgot-password Open Redirect via `x-forwarded-host`** — 신뢰 프록시 화이트리스트 없음 | `forgot-password/page.tsx:26-41` |
| M5 | **민감 fetch 로깅** — dev 모드에서 Supabase 요청 body(토큰 포함 가능) 콘솔 출력 | `SupabaseServerContext.tsx`, `SupabaseStorage.ts` |
| M6 | **`useState` 가 URL state 미러링** — `searchParams` 자체가 source of truth 인데 useEffect 로 베껴 동기화 | `users/page.tsx:565-575` 외 다수 |
| M7 | **앱 테이블 RLS 0건** — `core.*`, `modules.*` 18개 테이블 모두 `rowsecurity = false`. anon key 노출 시 전부 노출 | 최소 `ENABLE ROW LEVEL SECURITY` + service_role bypass |
| M8 | **로깅 비표준** — `console.log/error` 142건, request-id/level 없음 | pino + `@repo/core/logger` 래퍼 |
| M9 | **마이그레이션 history phantom rows 9개** — chemeng 시점 rename 잔재 | `DELETE FROM migrate.history WHERE id NOT IN (...)` 또는 정책 문서화 |
| M10 | **`modules.files` 의 `module` (string) vs `module_id` (uuid) 이원화** — 어느 게 source of truth 인지 불명 | CHECK constraint 또는 generated column |
| M11 | **`comments` 의 `parent_id` vs `head/arrange/depth` 공존** — 스레디드 표현 두 가지 동시 |  |
| M12 | **i18n 사실상 미사용** — `t()` import 1개, `registerDict()` 0건. 가이드 톤과 현실 괴리 | 점진 마이그레이션 가속 또는 가이드 톤다운 |
| M13 | **`apps/web/server.js` 의 `helmet` 미사용 + cors 와일드카드** | 프로덕션 위험 |
| M14 | **bootstrap-admin.ts 가 `--password` CLI 인자로 받음** — shell history 유출 | stdin prompt 권장 |
| M15 | **rolePermissions 캐시 TTL 2초** — 권한 회수 최대 2초 지연 | 위험도 낮지만 명시 |
| M16 | **`apps/web` `engines.node: >=20.9.0` vs 루트 `>=22` 불일치** | 통일 |
| M17 | **ESLint 룰 빈약** — `no-unused-imports` 만 실질 활성, `react-hooks/rules-of-hooks` 주석 처리됨, `no-explicit-any` / `no-floating-promises` 미적용 |  |
| M18 | **`module-sms`/`module-file` `as any` 다발** — SDK 타입 활용해 개선 가능 |  |
| M19 | **`new-module.mjs` 의 hyphen 이름 치환 버그** — `my-feature` → `Myfeature` (소문자만) |  |
| M20 | **`module-board/admin/page.tsx:26` 의 stale `console.log("modules", ...)`** | vite drop console 은 SSR 미적용 |

---

## 🟢 새로 도입 고려 (외주 운영 인프라 표준화)

비용 대비 ROI 순:

1. **Sentry** — 무료 tier 5k events/mo (외주 1개당 충분). `@sentry/react-router` SSR loader/action 자동 캡처. 4시간 도입. **ROI 최대**.
2. **GitHub Actions CI** — typecheck + test + eslint + syncpack 매트릭스. 외주마다 fork 시 즉시 재사용. 4시간.
3. **Renovate** — `pnpm-workspace.yaml catalog:` 정식 지원 (`pnpm: { catalogs: true }`). 주 1회 PR. 1시간.
4. **Knip / ts-prune** — 19개 패키지 모노레포 데드코드 자동 탐지 (H5 같은 거 자동 표면화). 1시간.
5. **Playwright E2E** — 로그인 / 보드 / 파일 업로드 골든 플로우 1세트. 외주별 회귀 방지. 1일.
6. **Audit log table** — `core.admin_audit` 신규. role 변경 / permission 변경 / hard delete 등 기록. 모든 외주에 필수. M.
7. **Rate limit 인프라** — Supabase RPC counter 또는 Upstash Redis. C7 의 근본 해결. M.
8. **super_admin 2FA (TOTP)** — Supabase Auth 의 MFA 활용 + `is_super_admin` 시 강제. M.
9. **CSP 헤더** — `entry.server.tsx` 에 nonce 기반 CSP. S.
10. **madge / depcruise** — 모듈 의존 graph CI 체크 (H6 자동화). S.
11. **공용 페이징 헬퍼 + `useUrlState` 훅** — 반복되는 `parseInt(searchParams.get("page") || "1")` 패턴 통일. S.
12. **`JsonResponse` 도메인 에러 헬퍼** — `BoardService.ts` 등의 `throw new Error("...")` 일관 매핑. S.
13. **chemeng-cleanup 마커** — `// TODO(template):` 주석 통일로 외주 시작 시 grep 한 번. S.
14. **소프트 삭제 표준화** — `core.deleted_modules` 만 별도 테이블. documents / comments 도 동일 패턴으로 통일. M.

---

## ✅ 유지 / 긍정

- 부트스트랩 마이그레이션 `core/000_init.sql` + `module-board/001_init.sql` 의 멱등 패턴
- `custom_access_token_hook` 의 `SECURITY DEFINER` + `SET search_path = ''` + `REVOKE FROM authenticated, anon, public`
- `createModule().permissions().roles().adminMenuItemUnits().build()` fluent builder + `InferPermissionName` 타입 추론
- `ENABLED_MODULES` 화이트리스트 + `ALWAYS_ON` 패턴
- 휴대폰 인증 토큰을 **httpOnly + SameSite=Lax + Secure(prod)** 쿠키로만 전달, HMAC `timingSafeEqual`
- 시간 컬럼 100% `timestamp with time zone`
- 게시판 정렬 인덱스 (`idx_documents_listing`) 가 정확히 커버
- 마이그레이션 트랜잭션 + `migrate.history` 멱등
- 한국 외주 적합한 `vercel.json: regions: ["icn1"], fluid: true`
- `BoardService.getDocuments` 의 `include` 로 N+1 방지
- syncpack + ESLint 의 모듈간 의존성 이중 차단
- `JsonResponse` 표준 응답 + 단위 테스트
- forgot-password user enumeration 방지 (`success redirect`)
- module-file `delete.ts` 의 ownership + super_admin 체크
- module-board write/edit 의 access/write 권한 이중 체크
- Prisma 거의 모든 곳에서 parameterized query (`$queryRawUnsafe` 는 menu.tsx 1건 뿐)

---

## 권장 처리 순서

### 1주차 — Critical (출시 막는 것)

- **C1** (admin 권한 가드) + **C3** (`$queryRawUnsafe` 제거)
- **C2** (`AUTH_SECRET` startup throw) + **C4** (MD5 fallback 제거)
- **C5** (`handle_new_user_minimal` 권한)
- **C6** (`identifiers` UNIQUE) — 마이그레이션 한 줄

### 2주차 — High (외주 출시 기본기)

- **H1** (CI) + **H2** (pre-commit) + **H7** (헬스체크) — DevOps 4시간 묶음
- **H3** (Sentry) — 별도
- **H8 / H9 / H11 / H12** (DB advisor 권고) — 마이그레이션 1건으로 묶기
- **H10** (turbo globalEnv)

### 한 달 내 — High 남은 것 + Medium 핵심

- **H4** (chemeng 컨텐츠) + **H5** (permissions.ts) + **H6** (auth↔sms 분리) — 디자인 시스템 작업과 묶기
- **M3** (이메일 동기화), **M4** (Host header), **M14** (bootstrap-admin stdin)
- 새로 도입: **Renovate, Knip, audit log table**

---

## 영역별 원본 감사 결과 요약

| 영역 | 발견 핵심 |
|---|---|
| **보안 / 인증 / 권한** | admin 권한 가드 부재가 결정적. AUTH_SECRET fallback, MD5 fallback, `$queryRawUnsafe`, handle_new_user_minimal RPC 노출이 합쳐서 escalation 체인 가능 |
| **아키텍처 / 코드 품질 / 데드코드** | chemeng 컨텐츠 잔재 광범위 (홈/about/멤버십/footer). permissions.ts 478줄 데드. auth↔sms 역방향 의존. 거대 파일 4개 (UserTable, sign-up, mypage, users) |
| **DB / 마이그레이션** | identifiers UNIQUE 부재가 가장 위험. FK 인덱스 5개 누락. `(user_id, is_active)` 부분 인덱스 부재. 앱 테이블 RLS 0건. 트리거 함수 search_path mutable |
| **DevOps / 관측성** | CI 0개. pre-commit 0개. Sentry 0개. rate limit 0개. helmet 미사용. 로깅 비표준. turbo globalEnv 불완전 |
